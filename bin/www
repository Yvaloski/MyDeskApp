#!/usr/bin/env node

/**
 * Module dependencies.
 */
const { startServer } = require('../app');
const debug = require('debug')('mydeskapp:server');

// Démarrer le serveur
startServer().catch(error => {
  console.error('Erreur lors du démarrage du serveur:', error);
  process.exit(1);
});
